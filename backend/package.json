{
  "name": "backend",
  "version": "1.0.0",
  "description": "just",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serverNode": "node ./server.js",
    "server": "nodemon ./server.js",
    "json-server": "json-server --watch users.json --port 5000",
    "client": "react-scripts start",
    "start:dev": "concurrently \"npm start\" \"npm run json-server\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "json-server": "^0.17.0"
  }
}
